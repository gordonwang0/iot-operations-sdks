apiVersion: akri.iotoperations.azure.com/v1
kind: ConnectorTemplate
metadata:
  name: mgmt-action-ctemplate
  namespace: azure-iot-operations
spec:
  deviceInboundEndpointTypes:
  - endpointType: mgmt-action-endpoint-type
    version: "0.0.1"
  runtimeConfiguration:
    runtimeConfigurationType: managedConfiguration
    managedConfigurationSettings:
      managedConfigurationType: imageConfiguration
      imageConfigurationSettings:
        imageName: "managementactionconnector"
        imagePullPolicy: Never # This sample image is built locally, so no need to pull
        replicas: 1
        tagDigestSettings:
          tagDigestType: tag
          tag: "latest"
  mqttConnectionConfiguration:
    host: "aio-broker:18883"
    authentication:
      method: ServiceAccountToken
      serviceAccountTokenSettings:
        audience: aio-internal
    tls:
      mode: Enabled
      trustedCaCertificateConfigMapRef: azure-iot-operations-aio-ca-trust-bundle
